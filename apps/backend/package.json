{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cargo run",
    "build": "cargo build --release",
    "lint": "cargo fmt --check",
    "type-check": "cargo check",
    "test": "cargo test",
    "clean": "cargo clean"
  }
}
